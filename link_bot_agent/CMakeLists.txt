cmake_minimum_required(VERSION 2.8.3)
project(link_bot_agent)

find_package(catkin REQUIRED COMPONENTS rospy)

catkin_package()

#catkin_install_python(PROGRAMS src/ DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION})
#catkin_python_setup()
